# Emacs
*~
\#*\#
.\#*
*.elc

# Python bytecode (the broker is a plain script)
__pycache__/
*.pyc

# Scratch conversation buffers
/diary.md

# Local git worktrees
/.worktrees/

# Vendored dependencies for offline byte-compile / ERT (not shipped;
# real installs get these via Package-Requires and straight/package.el)
/.deps/
/run-tests.sh
